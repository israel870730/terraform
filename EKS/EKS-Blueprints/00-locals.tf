locals {
#   cluster_name = "${var.env}-${var.region}-${var.project_name}"
#   name         = "demo"
#   ng_name      = "${var.env}-${var.project_name}"
#   region       = var.region
#   bucket_name  = "${var.env}-${var.region}-${var.project_name}-eks-pod-log"
   tags = var.tags
#   provisionerrole = var.provisionerrole                         
}